<style>
  :root{ --border:#000; }
  *{font-family:system-ui,-apple-system,"Segoe UI",Roboto,sans-serif; margin:0; padding:0; box-sizing:border-box;}
  .layout{position:relative; height:100%; width:100%; overflow:hidden;}
  .map{height:100%; width:100%}
  .craft{position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); z-index:900;}
  .craft img{width:48px; height:48px; filter:brightness(0)}
  .mini-pill { position:absolute; left:8px; right:8px; bottom:8px; z-index:1000; background:#fff; border:1.5px solid var(--border); border-radius:12px; padding:8px 10px; display:flex; align-items:center; justify-content:center; min-height:40px; }
  .text-content { font-weight:800; font-size:18px; line-height:1.1; text-align:center; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; width:100%; color:#000; }
</style>
